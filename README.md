## Slackbot Sonos

A Slackbot for playing with SONOS in Slack.

## Installation & Set Up
Create an `Incoming Webhook` in Slack.

Clone, Fork or Download this package.

Run an NPM Install.
```
$ npm install
```

Copy `config-sample.js` to `config.js` and configure as required.
